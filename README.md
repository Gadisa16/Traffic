Taxi / Vehicle Registration & Inspection — Monorepo

This workspace contains the MVP components for the Taxi Registration & Inspection system:

- `backend/` — FastAPI backend (API, models, schemas)
- `web-admin/` — React admin dashboard (placeholder)
- `mobile-inspector/` — Expo React Native inspector app (placeholder)
- `docs/` — design and API notes (optional)

Start by setting up the backend (see `backend/README.md`).
